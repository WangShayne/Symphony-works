# Use a tracker-driven unattended service

Symphony will remain a tracker-driven, unattended development orchestration service rather than become primarily an interactive CLI or desktop assistant. This preserves the upstream project's workspace isolation, reconciliation, retry, and long-running orchestration model; future task-entry channels can be added without replacing that core.
