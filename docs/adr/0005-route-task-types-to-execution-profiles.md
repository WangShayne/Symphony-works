# Route task types to execution profiles

A task type will select a complete execution profile rather than only a model identifier. The profile owns the task model, instructions, available tools, permissions, budgets, concurrency limits, and acceptance rules, allowing specialized work to differ safely in execution behavior without hard-coding those differences into the orchestrator.
