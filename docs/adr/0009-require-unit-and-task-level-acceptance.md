# Require unit and task-level acceptance

An execution unit may be integrated only after its execution-profile acceptance rules pass, and a change request may become Ready for Review only after task-level acceptance passes on the task integration branch. Model completion claims are advisory, and acceptance does not mean task completion; completion requires observing the subsequent human-authorized merge.
