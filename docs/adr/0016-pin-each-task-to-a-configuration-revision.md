# Pin each task to a configuration revision

Every tracked development task pins the active configuration revision when execution begins. Activation of a newer revision affects only new tasks; existing tasks keep the same routing, profiles, models, and acceptance rules through rescheduling and completion. Moving an active task to another revision requires an explicit operator migration or restart.
