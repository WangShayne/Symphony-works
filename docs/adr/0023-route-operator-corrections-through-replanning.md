# Route operator corrections through replanning

Operators may pause, cancel, or retry tasks and may submit audited constraints or corrections, but cannot directly edit an active plan document. An operator instruction creates a rescheduling event, and only the routing model may turn it into a schema-valid plan revision. This preserves human control without bypassing validation or history.
