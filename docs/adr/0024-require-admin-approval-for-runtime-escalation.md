# Require administrator approval for runtime escalation

An execution unit that needs a tool, command, path, or network capability outside its pinned execution profile may create a permission request but cannot grant itself access. An Administrator may approve a one-time, narrowly scoped capability; request, decision, and use are audited. An unanswered request remains blocked or fails according to its configured timeout policy.
