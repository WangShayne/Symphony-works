# Notify interventions through Dashboard and webhooks

V1 emits real-time Dashboard and configurable webhook notifications for permission requests, hard budget limits, persistent failures, invalid configuration, and blocked tasks. Slack, Teams, Feishu, and similar services integrate through the generic webhook rather than dedicated V1 adapters; routine progress remains in the Dashboard and Progress Comment.
