# Limit each task to one repository

V1 assigns each tracked development task exactly one repository. Its execution workspaces, task integration branch, acceptance, and change request remain within that repository. Cross-repository work is represented by linked Issues with separate plans and delivery lifecycles, avoiding ambiguous partial completion and rollback.
