# Honor tracker dependencies as hard constraints

An unfinished provider-declared blocker prevents a tracked development task from entering execution, and dependency completion triggers automatic reevaluation. Routing models may define dependencies among execution units but cannot create, remove, or override dependencies between external Issues.
