# Use automation projects as the top-level boundary

An Automation Project binds one tracker scope to one repository and target branch together with routing and fallback models, task types, execution profiles, budgets, and task-level acceptance. GitHub and GitLab Issues may imply their repository, while Linear uses an explicit project-to-repository mapping. Tasks are configured and scheduled only within this boundary.
