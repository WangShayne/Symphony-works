# Pin a task baseline commit

Planning, execution workspaces, and the task integration branch derive from one target-branch commit pinned when the task starts. Later target-branch changes do not enter silently; updating the baseline is an explicit, audited orchestration operation, and conflicts create an integration execution unit.
