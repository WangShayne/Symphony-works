# Clean up workspaces by terminal outcome

After an observed successful merge, execution containers stop and local worktrees are removed immediately. Failed or cancelled workspaces become read-only and remain for a configurable period of seven days by default before cleanup. Remote branches and change requests follow Automation Project and provider policy rather than unconditional deletion.
