# Target a single Linux Docker host

V1 production deployment targets one Linux server with Docker Engine, an official application image, and Docker Compose. Execution sandboxes run on that same Docker host. macOS and Linux remain supported for development, while Kubernetes, Windows Server, and remote execution runners are outside V1.
