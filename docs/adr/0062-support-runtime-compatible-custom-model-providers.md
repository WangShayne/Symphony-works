# Support Runtime-compatible custom model providers

Administrators may configure built-in or custom model providers only when their protocol is compatible with the selected Agent Runtime, which in V1 means Codex App Server compatibility. Model references include endpoint, model ID, secret reference, context window, capabilities, and pricing metadata, and activation probes connectivity and structured-output behavior. V1 does not promise compatibility with arbitrary chat APIs.
