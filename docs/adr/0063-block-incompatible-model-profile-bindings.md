# Block incompatible model-profile bindings

Configuration activation rejects a model binding when the model lacks or cannot verify a capability required by its Routing or Execution Profile. Structured-plan behavior is mandatory for routing, while execution profiles may require tool use, context size, vision, or other declared capabilities. Warnings cannot override a missing required capability.
