# Pin model prices for budget accounting

Monetary budgets require configured input, cached-input, and output prices; self-hosted or free models may declare zero. A task's pinned configuration revision also pins its prices so later administrative changes do not rewrite historical or in-flight cost. Unpriced models may use token and elapsed-time budgets but cannot claim monetary-budget enforcement.
