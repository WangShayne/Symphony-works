# Set a single-host V1 capacity baseline

V1 acceptance targets 100 active tracked tasks, 20 concurrent execution units, 10,000 retained task records, and sub-two-second P95 response for common Dashboard pages on a reasonably provisioned Linux host. Administrators may configure lower concurrency, and actual model throughput remains provider- and hardware-dependent.
