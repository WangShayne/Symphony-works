# Provide an operations-focused CLI

V1 includes a CLI for service startup, health checks, database migration, backup and restore, configuration import and export, and one-time Administrator bootstrap. Task controls call the versioned REST API, and the CLI does not become an interactive coding or prompt-submission surface.
